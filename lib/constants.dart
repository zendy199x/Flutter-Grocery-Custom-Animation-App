import 'package:flutter/material.dart';

const double defaultPadding = 20.0;
const double cartBarHeight = 100.0;
const double headerHeight = 85.0;

const Color bgColor = Color(0xFFF6F5F2);
const Color primaryColor = Color(0xFF40A944);

const Duration panelTransition = Duration(milliseconds: 500);
